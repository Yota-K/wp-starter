## 起動
`$ git clone https://github.com/Yota-K/wordpress-starter`
`$ docker-compose build`
`$ docker-compose up -d`

## アクセス
localhost:8080

## コンテナを止める
$ docker-compose stop

## コンテナを消す
$ docker-compose down

## DBのデータを消す
$ docker volume ls
